# livel2 ==> level13 :

## Log in to the level : 
    - user : **level12** :
    - password : **password is : Cron job ** :

### 📚 Commands You'll Need
- `cd`: Change directory
- `ls`: List files and directories
- `pwd`: Print working directory
- `cat`: Concatenate and display file contents .
- `tab` key: Autocomplete file names .
- `grep`: Search for patterns within files.
- `|` : Passing the result from one command to another .
- `cp` : copy files and directories
- `openssl` OpenSSL command line program.
- `sudo` : Execute commands with superuser privileges
- `passwd`: Change user passwords
- `ifconfig`: Display network interface configuration
- `su`: Switch user
- `crontab` : tables for driving cron .


### 🏁 The Challenge
Your mission, should you choose to accept it, is to find a scheduled task that 
contains sensitive information. The password for level13 is hidden in a cron job.

**the solution method**
- The session changed to level 13

**Here's what you need to do**:
- Look for cron job files in the system
- Check the contents of these files
- Look for any commands that might contain passwords
- The password for level13 is hidden in one of these commands

## Lessons Learned
- In this lesson, I explored the concept of cron in the Linux system.
## About Me
- [linkedin](https://ma.linkedin.com/in/hamza-el-ansary-799368386)
- [github](https://github.com/settings/profile)

